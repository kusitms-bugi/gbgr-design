{
	"name": "gbgr-design",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "turbo run build --filter=@gbgr/* && pnpm --filter docs build",
		"postbuild": "node scripts/sync-vercel-output.mjs",
		"dev": "turbo run dev",
		"test": "turbo run test",
		"docs:dev": "pnpm --filter docs dev",
		"docs:build": "pnpm --filter docs build",
		"lint": "turbo run lint",
		"check": "biome check . && pnpm check:rules",
		"check:rules": "node scripts/validate-react-rules.mjs",
		"check:write": "biome check . --write",
		"changeset": "changeset",
		"changeset:version": "changeset version",
		"changeset:publish": "changeset publish",
		"typecheck": "pnpm --filter @gbgr/icons --filter @gbgr/react-headless --filter @gbgr/react run build && turbo run typecheck",
		"release": "turbo run build --filter=@gbgr/* && changeset publish",
		"storybook": "storybook dev -p 6006",
		"build-storybook": "storybook build"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"@changesets/changelog-git": "^0.2.1",
		"@changesets/cli": "latest",
		"@storybook/react-vite": "^10.3.5",
		"@testing-library/react": "^16.2.0",
		"@testing-library/user-event": "^14.6.1",
		"@types/react": "latest",
		"@types/react-dom": "latest",
		"jsdom": "^26.0.0",
		"react": "^19.2.5",
		"react-dom": "^19.2.5",
		"storybook": "^10.3.5",
		"turbo": "latest",
		"typescript": "latest",
		"vitest": "^3.2.4"
	},
	"packageManager": "pnpm@10.28.1",
	"engines": {
		"node": ">=18.0.0",
		"pnpm": ">=8.0.0"
	},
	"pnpm": {
		"overrides": {
			"react": "^19.2.5",
			"react-dom": "^19.2.5"
		}
	}
}
